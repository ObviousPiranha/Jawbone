namespace Piranha.Jawbone.Sdl3;

public enum SdlGpuPresentMode // SDL_GPUPresentMode
{
    Vsync = 0, // SDL_GPU_PRESENTMODE_VSYNC
    Immediate = 1, // SDL_GPU_PRESENTMODE_IMMEDIATE
    Mailbox = 2, // SDL_GPU_PRESENTMODE_MAILBOX
}
